from setuptools import setup, find_packages

setup(
    name='ovp8xxexamples',
    version='0.1',
    description='Python examples for ifm OVP8xx devices',
    packages=find_packages(),  # finds packages under 'src'
)